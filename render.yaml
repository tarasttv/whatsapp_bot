services:
  - type: web
    name: whatsapp-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 app.py"
    envVars:
      - key: PORT
        value: 5000
